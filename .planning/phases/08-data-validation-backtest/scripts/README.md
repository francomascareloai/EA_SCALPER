# ⚠️ Não colocar scripts aqui

Esta pasta existe apenas para evitar que ferramentas criem `.py` dentro de `.planning/`.

- Scripts executáveis devem ficar em `scripts/` (repo) ou `nautilus_gold_scalper/scripts/` (robô).
- Outputs JSON continuam em `.planning/phases/08-data-validation-backtest/outputs/`.

Scripts migrados:
- `task_1a1_tick_count.py` → `scripts/data/phase1a_tick_count.py`
- `gap_detection_v2.py` → `scripts/data/phase1a_gap_detection.py`

