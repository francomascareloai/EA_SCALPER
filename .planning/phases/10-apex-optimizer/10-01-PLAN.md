---
phase: 10-apex-optimizer
plan: 01
type: execute
---

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
# OBRIGATORIO apos qualquer mudanca de codigo
./.venv/bin/pytest -q

# Typecheck (escopo minimo do que foi mexido)
./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization
```

### 3. Parallel Agents (sem limite)
- Pode spawnar multiplos agents em paralelo para fixes

### 4. Anti-Hallucination
- NUNCA inventar APIs: sempre verificar assinaturas no codigo local
- NUNCA dizer "deve funcionar" sem rodar os checks acima

### 5. Consultar Documentacao
```bash
# Verificar APIs locais antes de codar
rg -n "class BacktestRunner" nautilus_gold_scalper/scripts/backtest/run_backtest.py
```

<objective>
Integrar o Apex Optimizer com o backtest real (NautilusTrader) via `BacktestRunner`, produzindo um `backtest_fn` compatível com `ApexOptimizer`.

Purpose: Tirar o optimizer do estado "MVP sem execução" e permitir rodar otimização end-to-end (mesmo que em janela curta / mock em testes).
Output: CLI funcional com backtest adapter + testes de integração leves (sem rodar tick backtest completo em CI).
</objective>

<execution_context>
@.planning/phases/10-apex-optimizer/00-MASTER.md
@DOCS/02_IMPLEMENTATION/APEX_OPTIMIZER_PRD.md
@CLAUDE.md
</execution_context>

<context>
@nautilus_gold_scalper/src/optimization/optimizer.py
@nautilus_gold_scalper/src/optimization/__main__.py
@nautilus_gold_scalper/scripts/backtest/run_backtest.py
@nautilus_gold_scalper/tests/test_integration/test_tick_backtest_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar adapter `backtest_fn` baseado no `BacktestRunner`</name>
  <files>nautilus_gold_scalper/src/optimization/backtest_adapter.py, nautilus_gold_scalper/src/optimization/optimizer.py</files>
  <action>
Criar um módulo `backtest_adapter.py` que exponha uma função/fábrica que gere um `backtest_fn(params, start_date, end_date) -> (trades_df, equity_series)`.

Requisitos mínimos:
- `trades_df` deve conter pelo menos: `timestamp` (datetime UTC) e `pnl` (float).
- `equity_series` deve ser um `pd.Series` indexado por datetime (ou compatível com o cálculo de DD em `wfa_inline.py`).
- O adapter deve traduzir `params` (dotpath) para o formato esperado pelo backtester (sem alterar a semântica de risco/tempo).

Evitar:
- Rodar backtests longos por padrão; o adapter deve aceitar range (start/end) para permitir smoke tests curtos.
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_integration/test_tick_backtest_e2e.py</verify>
  <done>Existe `backtest_fn` funcional e tipado, e `ApexOptimizer` consegue chamar o backtest via adapter (com stub/mocks em teste) sem quebrar.</done>
</task>

<task type="auto">
  <name>Task 2: Remover placeholder do CLI e executar otimização de verdade quando possível</name>
  <files>nautilus_gold_scalper/src/optimization/__main__.py</files>
  <action>
Atualizar o CLI para:
- Manter `--dry-run` como hoje.
- Em execução normal, construir `ApexOptimizer` e injetar o `backtest_fn` do adapter.
- Expor flags opcionais para facilitar smoke tests (ex.: `--train-start`, `--train-end`) SEM precisar editar YAML.

Evitar:
- Alterar contratos públicos sem necessidade; preferir adição de flags.
  </action>
  <verify>python -m nautilus_gold_scalper.src.optimization --config nautilus_gold_scalper/configs/grids/smc_optimization.yaml --dry-run</verify>
  <done>CLI não imprime mais “NOTE: integrate backtest runner” como único caminho; existe caminho real de execução (ainda que com range curto).</done>
</task>

<task type="auto">
  <name>Task 3: Criar testes leves de integração (sem backtest completo)</name>
  <files>nautilus_gold_scalper/tests/test_optimization/test_backtest_adapter_smoke.py</files>
  <action>
Adicionar teste que valida o contrato do `backtest_fn` sem rodar tick backtest pesado:
- Mockar/monkeypatch o `BacktestRunner` ou a função que ele usa para retornar (trades_df, equity_series) sintéticos.
- Garantir que `ApexOptimizer._objective_fn` consome isso e produz `TrialResult` consistente.

Evitar:
- Dependência de dados reais ou execução longa.
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_backtest_adapter_smoke.py</verify>
  <done>Teste passa e valida o contrato `timestamp/pnl/equity_series` de ponta a ponta (com stub).</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest -q`
- [ ] `./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization`
- [ ] `python -m nautilus_gold_scalper.src.optimization --config nautilus_gold_scalper/configs/grids/smc_optimization.yaml --dry-run`
</verification>

<success_criteria>
- CLI pode executar o optimizer com um `backtest_fn` real (via adapter) ou, no mínimo, com stub validado em testes.
- Não há regressão em `--dry-run`.
- Testes e mypy estrito passam.
</success_criteria>

<output>
After completion, create `.planning/phases/10-apex-optimizer/10-01-SUMMARY.md`:

# Phase 10 Plan 01: Backtest Integration Summary

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
</output>
