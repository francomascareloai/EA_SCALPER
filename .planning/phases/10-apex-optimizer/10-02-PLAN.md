---
phase: 10-apex-optimizer
plan: 02
type: execute
---

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
./.venv/bin/pytest -q
./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization
```

<objective>
Implementar os modos `grid` e `random` (Latin Hypercube) no `ApexOptimizer`, reutilizando `ParameterSpec`/dotpath e respeitando `max_grid_size`.

Nota: `optuna` pode não estar instalado no ambiente atual; estes modos não dependem de Optuna e devem funcionar mesmo sem `optuna`.

Purpose: Permitir exploração não-bayesiana (debug/ablation) e reduzir risco de “Optuna only”.
Output: `search/grid.py` + `search/random.py` + wiring em `optimizer.py` + testes.
</objective>

<execution_context>
@.planning/phases/10-apex-optimizer/00-MASTER.md
@DOCS/02_IMPLEMENTATION/APEX_OPTIMIZER_PRD.md
</execution_context>

<context>
@nautilus_gold_scalper/src/optimization/config.py
@nautilus_gold_scalper/src/optimization/search/base.py
@nautilus_gold_scalper/src/optimization/search/bayesian.py
@nautilus_gold_scalper/src/optimization/optimizer.py
@nautilus_gold_scalper/src/optimization/__main__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar Grid Search com cap de segurança</name>
  <files>nautilus_gold_scalper/src/optimization/search/grid.py, nautilus_gold_scalper/src/optimization/optimizer.py</files>
  <action>
Criar `GridSearch(SearchStrategy)` que:
- Gera o produto cartesiano dos parâmetros (range+step ou choices).
- Calcula tamanho do grid ANTES de executar e aplica `max_grid_size` como hard cap (fail-fast com mensagem clara).
- Para floats/ints: respeitar `step`; para ints: garantir cast correto.
- Respeitar ordem determinística para reprodutibilidade.

Evitar:
- Gerar grid em memória se for grande; usar iterador/lazy quando possível.
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_grid_search.py</verify>
  <done>Modo `grid` roda sem explodir memória e bloqueia grids acima do cap com erro claro.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar Random Search via Latin Hypercube (LHS) reprodutível</name>
  <files>nautilus_gold_scalper/src/optimization/search/random.py, nautilus_gold_scalper/src/optimization/optimizer.py</files>
  <action>
Criar `RandomSearch(SearchStrategy)` que:
- Implementa LHS simples com `seed` para reprodutibilidade.
- Para floats: amostrar por quantis estratificados + permutação.
- Para ints: amostrar no domínio inteiro, estratificando quando viável.
- Para categorical: amostrar choices uniformemente.

Evitar:
- Dependência nova pesada (não adicionar scikit-optimize etc.).
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_random_search.py</verify>
  <done>Modo `random` gera N amostras, respeita seed, e passa pelas mesmas validações/constraints do pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar CLI para suportar `--mode grid|random` end-to-end</name>
  <files>nautilus_gold_scalper/src/optimization/__main__.py</files>
  <action>
Garantir que o CLI permite `--mode grid` e `--mode random` (override do YAML), e que `--dry-run` continue reportando grid size estimado quando aplicável.
  </action>
  <verify>python -m nautilus_gold_scalper.src.optimization --config nautilus_gold_scalper/configs/grids/smc_optimization.yaml --mode grid --dry-run</verify>
  <done>CLI aceita modos `grid` e `random` sem crash e com comportamento esperado.</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest -q`
- [ ] `./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization`
- [ ] `python -m nautilus_gold_scalper.src.optimization --config nautilus_gold_scalper/configs/grids/smc_optimization.yaml --mode grid --dry-run`
</verification>

<success_criteria>
- `optimizer.run()` funciona para `grid` e `random`.
- `max_grid_size` é respeitado como hard cap.
- Testes e mypy estrito passam.
</success_criteria>

<output>
After completion, create `.planning/phases/10-apex-optimizer/10-02-SUMMARY.md`.
</output>
