---
phase: 10-apex-optimizer
plan: 03
type: execute
---

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
./.venv/bin/pytest -q
./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization
```

<objective>
Consolidar a semântica de constraints (hard reject vs penalty) e alinhar com Optuna + Apex rules.

Purpose: Evitar inconsistência entre `ApexConstraintChecker.score_penalty`, `constraint_fn` e o hard reject `-999`.
Output: Constraint function consistente + testes.
</objective>

<execution_context>
@.planning/phases/10-apex-optimizer/00-MASTER.md
@DOCS/02_IMPLEMENTATION/APEX_OPTIMIZER_PRD.md
@CLAUDE.md
</execution_context>

<context>
@nautilus_gold_scalper/src/optimization/optimizer.py
@nautilus_gold_scalper/src/optimization/search/bayesian.py
@nautilus_gold_scalper/src/optimization/constraints/apex.py
@nautilus_gold_scalper/src/optimization/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Padronizar constraints como hard gates (Optuna <=0 satisfied)</name>
  <files>nautilus_gold_scalper/src/optimization/optimizer.py, nautilus_gold_scalper/src/optimization/search/bayesian.py, nautilus_gold_scalper/src/optimization/constraints/apex.py</files>
  <action>
Definir claramente:
- Apex compliance é **constraint dura**: violou → trial rejeitada (score muito negativo / prune).
- Validation gates mínimas (WFE, trades, etc.) são **gates duras** para pruning.
- `score_penalty` fica reservado para “near-limit” (soft) e NUNCA deve permitir passar uma violação dura.

Garantir que:
- `constraint_fn` retorne valores no formato Optuna (<=0 ok; >0 viola).
- O caminho de execução em `bayesian.py` e nos outros searchers use o mesmo critério.

Evitar:
- Duplicar a lógica em múltiplos lugares; centralizar no checker quando possível.
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_constraints_semantics.py</verify>
  <done>Para qualquer violação Apex/validation: o trial é rejeitado/pruned de forma consistente em todos os modos.</done>
</task>

<task type="auto">
  <name>Task 2: Cobrir edge cases com testes (inclui ranges e limites)</name>
  <files>nautilus_gold_scalper/tests/test_optimization/test_constraints_semantics.py</files>
  <action>
Criar testes unitários que cobrem:
- trailing_dd exatamente no limite e ligeiramente abaixo/acima
- daily_profit_max no limite
- time_gate_violations e overnight_positions > 0
- wfe/trades abaixo do mínimo

Os testes devem validar tanto `get_constraint_values()` quanto o comportamento do searcher (ex.: retorna -999 / pruned).
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_constraints_semantics.py</verify>
  <done>Testes passam e impedem regressões na semântica de constraints.</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest -q`
- [ ] `./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization`
</verification>

<success_criteria>
- Existe uma única semântica consistente para constraints.
- Trials Apex-não-compliant nunca “passam” por penalty.
</success_criteria>

<output>
After completion, create `.planning/phases/10-apex-optimizer/10-03-SUMMARY.md`.
</output>
