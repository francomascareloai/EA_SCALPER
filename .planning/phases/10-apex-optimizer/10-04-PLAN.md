---
phase: 10-apex-optimizer
plan: 04
type: execute
---

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
./.venv/bin/pytest -q
./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization
```

<objective>
Integrar Monte Carlo (block bootstrap) como Layer 3 do Apex Optimizer (top N apenas), com output em reports/handoff.

PLUS (mesma motivação de falsificação-first): executar **Ghost Test baseline** pós-otimização (top N) para atribuir edge (sinal vs filtros).

Purpose: Estimar tail risk (MC95DD/MC99DD) e filtrar configs frágeis antes do GO/NO-GO, e verificar se a “edge” vem do componente sob teste.
Output: Stress runner + campos no TrialResult + reporting atualizado + seção Ghost Test no handoff.
</objective>

<execution_context>
@.planning/phases/10-apex-optimizer/00-MASTER.md
@DOCS/02_IMPLEMENTATION/APEX_OPTIMIZER_PRD.md
</execution_context>

<context>
@scripts/oracle/monte_carlo.py
@nautilus_gold_scalper/src/optimization/optimizer.py
@nautilus_gold_scalper/src/optimization/search/base.py
@nautilus_gold_scalper/src/optimization/reporting/summary.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar integração de Monte Carlo (top N) no pipeline</name>
  <files>nautilus_gold_scalper/src/optimization/stress/monte_carlo.py, nautilus_gold_scalper/src/optimization/optimizer.py, nautilus_gold_scalper/src/optimization/search/base.py</files>
  <action>
Criar um wrapper de stress test que reutilize `scripts.oracle.monte_carlo.BlockBootstrapMC` (preferido) ou implemente versão mínima no módulo.

Requisitos:
- Rodar somente para `top_n` resultados.
- Adaptar o input esperado (`profit` vs `pnl`) sem quebrar `wfa_inline.py`.
- Persistir pelo menos `mc_95_dd` e `mc_99_dd` no resultado do trial (campos opcionais).

Evitar:
- Rodar MC dentro do objective_fn (isso explode custo por trial). Deve ser pós-filtro.
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_monte_carlo_integration.py</verify>
  <done>Após otimização, os top N trials têm MC95/MC99 preenchidos quando stress_test.enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar reports/handoff para incluir métricas de stress</name>
  <files>nautilus_gold_scalper/src/optimization/reporting/summary.py</files>
  <action>
Atualizar `SummaryReporter` para incluir:
- mc_95_dd, mc_99_dd
- qualquer metadado útil (simulações, block_size, etc.) se disponível

Manter compatibilidade com JSON/CSV existentes.
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_reporting_stress_fields.py</verify>
  <done>Reports incluem stress fields e continuam gerando sem erro.</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest -q`
- [ ] `./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization`
</verification>

<success_criteria>
- Monte Carlo roda apenas para top N.
- Métricas entram nos reports/handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/10-apex-optimizer/10-04-SUMMARY.md`.
</output>
