---
phase: 10-apex-optimizer
plan: 05
type: execute
---

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
./.venv/bin/pytest -q
./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization
```

<objective>
Implementar os detectores anti-overfit do PRD (cliff, island, regime bias) e incluir no report/handoff.

Purpose: Detectar “otimizações suspeitas” (picos isolados/limites/régime) e reduzir falsos positivos.
Output: `anti_overfit.py` + integração com reporting.
</objective>

<execution_context>
@.planning/phases/10-apex-optimizer/00-MASTER.md
@DOCS/02_IMPLEMENTATION/APEX_OPTIMIZER_PRD.md
</execution_context>

<context>
@nautilus_gold_scalper/src/optimization/config.py
@nautilus_gold_scalper/src/optimization/search/base.py
@nautilus_gold_scalper/src/optimization/reporting/summary.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar cliff/island/regime-bias detectors</name>
  <files>nautilus_gold_scalper/src/optimization/constraints/anti_overfit.py</files>
  <action>
Criar implementações alinhadas ao PRD:
- `detect_cliff(best_params, param_ranges)` com tolerância ~5% do range.
- `detect_island(results, top_k, neighbor_threshold)` usando “vizinhos” por tolerância de parâmetro (ex.: 10%).
- `detect_regime_bias(result, min_coverage)` usando `regime_scores` quando disponível.

Evitar:
- Requerer regime_scores sempre; se indisponível, degradar para “N/A” sem quebrar.
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_anti_overfit.py</verify>
  <done>Detectors retornam warnings determinísticos e cobertos por testes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar warnings no report/handoff</name>
  <files>nautilus_gold_scalper/src/optimization/reporting/summary.py, nautilus_gold_scalper/src/optimization/optimizer.py</files>
  <action>
Após a otimização (e stress, se houver), gerar e anexar ao output:
- Lista de warnings por trial (ou pelo menos para top N)
- Resumo agregado (counts por tipo: CLIFF_LOW/HIGH, ISLAND, REGIME_BIAS)

Manter formato de handoff do PRD (seção “Overfitting Analysis”).
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_reporting_overfit_fields.py</verify>
  <done>Handoff/report incluem warnings e não quebram execução quando lista vazia.</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest -q`
- [ ] `./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization`
</verification>

<success_criteria>
- Detectors implementados e reportados.
- Nenhuma quebra do pipeline quando informações faltam.
</success_criteria>

<output>
After completion, create `.planning/phases/10-apex-optimizer/10-05-SUMMARY.md`.
</output>
