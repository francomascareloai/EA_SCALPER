---
phase: 10-apex-optimizer
plan: 06
type: execute
---

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
./.venv/bin/pytest -q
./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization
```

<objective>
Finalizar o “produto” do Apex Optimizer: handoff estruturado para ORACLE/SENTINEL + checklist de execução e validação.

Purpose: Tornar o output consumível pelo pipeline de validação (ORACLE/SENTINEL) e reduzir retrabalho/manual.
Output: Handoff markdown consistente + melhoria do summary + documentação mínima de execução (via CLI help/messages).
</objective>

<execution_context>
@.planning/phases/10-apex-optimizer/00-MASTER.md
@DOCS/02_IMPLEMENTATION/APEX_OPTIMIZER_PRD.md
@CLAUDE.md
</execution_context>

<context>
@nautilus_gold_scalper/src/optimization/reporting/summary.py
@nautilus_gold_scalper/src/optimization/__main__.py
@scripts/oracle/go_nogo_validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Padronizar handoff para ORACLE e SENTINEL</name>
  <files>nautilus_gold_scalper/src/optimization/reporting/summary.py</files>
  <action>
Gerar handoff markdown no formato do PRD (seções: Run Metadata, Top N table, Apex Rejection Summary, Overfitting Analysis, Stress Results, Recommendations, Files Generated).

Requisitos:
- Suportar `target_agents: [ORACLE, SENTINEL]`.
- Incluir explicitamente limites Apex (buffers) e os campos calculados (trailing_dd, daily_profit_max, time_gate_violations, overnight_positions).

Evitar:
- Dump gigante; manter compacto (top N + contagens).
  </action>
  <verify>./.venv/bin/pytest -q nautilus_gold_scalper/tests/test_optimization/test_handoff_format.py</verify>
  <done>Handoff é gerado para ORACLE e SENTINEL sem erros e com seções mínimas preenchidas.</done>
</task>

<task type="auto">
  <name>Task 2: Fechar checklist de execução end-to-end no CLI</name>
  <files>nautilus_gold_scalper/src/optimization/__main__.py</files>
  <action>
Aprimorar mensagens/ajuda do CLI para mostrar um “happy path”:
- `--dry-run` para validar config
- execução com `--mode bayesian --trials N --parallelism K`
- onde encontrar reports/handoff

Evitar:
- Criar docs novas; preferir `--help` e prints do CLI.
  </action>
  <verify>python -m nautilus_gold_scalper.src.optimization --help</verify>
  <done>CLI orienta claramente como executar e onde estão os outputs, sem depender de conhecimento externo.</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest -q`
- [ ] `./.venv/bin/mypy --strict nautilus_gold_scalper/src/optimization`
- [ ] `python -m nautilus_gold_scalper.src.optimization --help`
</verification>

<success_criteria>
- Handoff pronto para consumo pelo ORACLE/SENTINEL.
- CLI descreve o caminho de execução e outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/10-apex-optimizer/10-06-SUMMARY.md`.
</output>
