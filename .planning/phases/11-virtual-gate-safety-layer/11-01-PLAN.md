---
phase: 11-virtual-gate-safety-layer
type: execute
---

# Phase 11-01 — Unified Safety Policy Surface (contract + wiring)

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
# OBRIGATORIO apos qualquer mudanca de codigo
pytest -q
mypy --strict
```

### 3. Anti-Hallucination
- NUNCA inventar APIs: verificar em `external/nautilus_trader/` ou no próprio repo via search.
- NUNCA dizer "passou" sem rodar os comandos.

## <objective>
Create a single, testable “policy surface” consumed by strategies before submitting any new entry orders, while guaranteeing **exit-always-allowed** (forced close bypass) and preserving existing risk/time/DD enforcement.

Purpose: Centralize gating/flattening logic so “most restrictive wins” is enforced consistently.
Output: Minimal new types/module + wiring points in strategy path.
</objective>

## <context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/PRD.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/TEST_CHECKLIST.md
@nautilus_gold_scalper/src/risk/time_constraint_manager.py
@nautilus_gold_scalper/src/risk/dd_protection.py
@nautilus_gold_scalper/src/risk/prop_firm_manager.py
@nautilus_gold_scalper/src/strategies/gold_scalper_strategy.py
@nautilus_gold_scalper/src/strategies/strategy_selector.py
</context>

## <tasks>

<task type="auto">
  <name>Task 1: Add UnifiedRiskPolicy types and decision contract</name>
  <files>nautilus_gold_scalper/src/risk/unified_risk_policy.py</files>
  <action>
Create a small module defining:
- A `RiskDecision` (or similarly named) immutable structure containing:
  - `can_open_new: bool`
  - `size_factor: float` (1.0 default; clamp to [0.0, 1.0])
  - `must_flatten: bool`
  - `reasons: list[str]` (or a single `halt_reason: str | None`)
- A `UnifiedRiskPolicy` class with a method like `evaluate(...) -> RiskDecision`.

Constraints:
- Decision semantics MUST encode: “must_flatten wins globally”; “entry gates are only about new entries”.
- Do NOT implement Titan-like ladder logic, cost averaging, or anything that triggers add-to-loser behavior.
- Keep it dependency-light and consistent with current project style.
</action>
  <verify>python -m compileall -q nautilus_gold_scalper/src/risk/unified_risk_policy.py</verify>
  <done>Module exists and is importable; types are mypy-friendly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire policy evaluation into strategy entry path (exit bypass preserved)</name>
  <files>nautilus_gold_scalper/src/strategies/gold_scalper_strategy.py</files>
  <action>
Identify the single choke point where new entry orders are created/submitted.

Implement the wiring:
- Evaluate `UnifiedRiskPolicy` before attempting ANY new entry.
- If `decision.must_flatten` is true: ensure forced close path executes and is NOT blocked by entry-only guards.
- If `decision.can_open_new` is false: skip entry submission.
- Apply `decision.size_factor` only to new entry sizing (never to exits).

Hard invariant:
- Force-close logic must bypass NewsGuard/ExposureCaps/VolatilitySpacing/VirtualGate.
</action>
  <verify>pytest -q</verify>
  <done>Entries are gated by policy; forced close remains possible regardless of entry gates.</done>
</task>

<task type="auto">
  <name>Task 3: Minimal unit tests for decision precedence</name>
  <files>nautilus_gold_scalper/tests/test_risk/test_unified_risk_policy.py</files>
  <action>
Add unit tests that assert:
- If `must_flatten=True`, system treats it as highest priority regardless of `can_open_new`.
- `size_factor` is clamped to [0, 1].

Keep tests isolated (no full backtest run here).
</action>
  <verify>pytest -q nautilus_gold_scalper/tests/test_risk/test_unified_risk_policy.py</verify>
  <done>Tests pass and protect precedence rules.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - `src/risk/unified_risk_policy.py` created
    - Strategy entry path calls unified policy
    - Unit tests for precedence
  </what-built>
  <how-to-verify>
    1. Run: `pytest -q`
    2. Run: `mypy --strict`
    3. Confirm: no new entry occurs when `can_open_new=False`, but exit/flatten code still runs.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

## <verification>
- [ ] `pytest -q`
- [ ] `mypy --strict`

## <success_criteria>
- A single policy surface exists and is used for entry gating.
- Forced close bypass is preserved (exit-always-allowed invariant).
- Unit tests cover precedence and size_factor bounds.

## <output>
After completion, create `.planning/phases/11-virtual-gate-safety-layer/11-01-SUMMARY.md`
</output>
