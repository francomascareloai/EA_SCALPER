---
phase: 11-virtual-gate-safety-layer
type: execute
---

# Phase 11-02 — ExposureCaps + NewsGuard + VolatilitySpacing (entry-only)

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
pytest -q
mypy --strict
```

## <objective>
Implement the three safest components first (ExposureCaps, NewsGuard, VolatilitySpacing) as **entry-only** gates and plug them into `UnifiedRiskPolicy`, ensuring **exit-always-allowed**.

Purpose: Reduce correlated blow-ups and avoid overtrading during news/volatility expansion.
Output: Small new modules + unit/integration tests from TEST_CHECKLIST.
</objective>

## <context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/PRD.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/TEST_CHECKLIST.md
@nautilus_gold_scalper/src/signals/news_calendar.py
@nautilus_gold_scalper/src/signals/news_trader.py
@nautilus_gold_scalper/src/risk/time_constraint_manager.py
@nautilus_gold_scalper/src/strategies/gold_scalper_strategy.py
@nautilus_gold_scalper/src/risk/unified_risk_policy.py
</context>

## <tasks>

<task type="auto">
  <name>Task 1: Implement ExposureCaps (counts + reason)</name>
  <files>nautilus_gold_scalper/src/risk/exposure_caps.py</files>
  <action>
Create an ExposureCaps component that can answer: “May we open a new position now?”

Minimum behavior:
- Enforce `max_concurrent_positions` and `max_concurrent_instruments`.
- Return structured result with allow/deny + reason.

Constraints:
- Must NOT block closing/flattening.
- Must be deterministic.
</action>
  <verify>nautilus_gold_scalper/.venv/bin/python -m compileall -q nautilus_gold_scalper/src/risk/exposure_caps.py</verify>
  <done>Module exists; can be used by unified policy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement NewsGuard (entry-only window math)</name>
  <files>nautilus_gold_scalper/src/risk/news_guard.py</files>
  <action>
Implement NewsGuard using the existing news calendar signal infrastructure.

Minimum behavior:
- Given decision time and news schedule, block new entries inside blackout windows.
- Exits and forced close are never blocked.

Constraints:
- Time math must be unambiguous and testable.
- Use canonical timezone rules already established elsewhere.
</action>
  <verify>nautilus_gold_scalper/.venv/bin/python -m compileall -q nautilus_gold_scalper/src/risk/news_guard.py</verify>
  <done>Entry-only blocking works; behavior is testable.</done>
</task>

<task type="auto">
  <name>Task 3: Implement VolatilitySpacing (monotone scaling)</name>
  <files>nautilus_gold_scalper/src/risk/volatility_spacing.py</files>
  <action>
Implement a spacing/cooldown gate that scales with volatility (ATR or ATR%).

Minimum behavior:
- Compute a required cooldown seconds given a volatility measure.
- Must be monotonic: higher volatility ⇒ higher spacing.
- Bounded by min/max.

Hard constraint:
- This is NOT “grid scheduling”. It must not re-enter just because price moved against an open trade.
</action>
  <verify>nautilus_gold_scalper/.venv/bin/python -m compileall -q nautilus_gold_scalper/src/risk/volatility_spacing.py</verify>
  <done>Spacing function monotonic and bounded.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate these gates into UnifiedRiskPolicy</name>
  <files>nautilus_gold_scalper/src/risk/unified_risk_policy.py</files>
  <action>
Extend `UnifiedRiskPolicy.evaluate(...)` to incorporate:
- ExposureCaps
- NewsGuard
- VolatilitySpacing

Composition rules:
- “Most restrictive wins” for entries (any deny ⇒ `can_open_new=False`).
- `must_flatten` remains highest priority global flag and bypasses entry-only gates.

Add reason strings so telemetry/logging can attribute the block.
</action>
  <verify>nautilus_gold_scalper/.venv/bin/python -m pytest -q nautilus_gold_scalper/tests</verify>
  <done>Unified policy returns correct combined decisions.</done>
</task>

<task type="auto">
  <name>Task 5: Add minimum unit tests from checklist (B3/B4/B5)</name>
  <files>
    nautilus_gold_scalper/tests/test_risk/test_news_guard.py
    nautilus_gold_scalper/tests/test_risk/test_exposure_caps.py
    nautilus_gold_scalper/tests/test_risk/test_volatility_spacing.py
  </files>
  <action>
Create unit tests matching TEST_CHECKLIST sections:
- B3: News window math blocks entries but not exits.
- B4: Exposure caps deny new entries when max reached but do not affect closing.
- B5: Volatility spacing is monotonic and bounded.

Keep tests fast and deterministic.
</action>
  <verify>nautilus_gold_scalper/.venv/bin/python -m pytest -q nautilus_gold_scalper/tests/test_risk/test_news_guard.py nautilus_gold_scalper/tests/test_risk/test_exposure_caps.py nautilus_gold_scalper/tests/test_risk/test_volatility_spacing.py</verify>
  <done>All unit tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - ExposureCaps + NewsGuard + VolatilitySpacing modules
    - UnifiedRiskPolicy integrates them as entry-only gates
    - Unit tests for B3/B4/B5
  </what-built>
  <how-to-verify>
    1. Run: `nautilus_gold_scalper/.venv/bin/python -m pytest -q nautilus_gold_scalper/tests`
    2. Run: `nautilus_gold_scalper/.venv/bin/mypy --config-file mypy.ini --strict [changed files]`
    3. Confirm: forced close path remains unblocked even when in news window.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

## <verification>
- [ ] `nautilus_gold_scalper/.venv/bin/python -m pytest -q nautilus_gold_scalper/tests`
- [ ] `nautilus_gold_scalper/.venv/bin/mypy --config-file mypy.ini --strict [changed files]`

## <success_criteria>
- Entry-only gates are implemented and enforced via UnifiedRiskPolicy.
- Exit-always-allowed invariant holds.
- Unit tests protect the gate math and monotonicity.

## <output>
After completion, create `.planning/phases/11-virtual-gate-safety-layer/11-02-SUMMARY.md`
</output>
