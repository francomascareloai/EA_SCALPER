---
phase: 11-virtual-gate-safety-layer
type: execute
---

# Phase 11-03 — VirtualGate (completed bars only, deterministic) + tests

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
pytest -q
mypy --strict
```

### 3. Anti-Hallucination (Nautilus APIs)
- Antes de integrar com NautilusTrader, verificar APIs por search em `external/nautilus_trader/examples` ou no repo.
- Se houver incerteza sobre timestamps/event time: STOP e valide com teste mínimo.

## <objective>
Implement `VirtualGate` as an observe-before-risk-on filter that only influences **new entry permission** and is:
- Temporal-correct: uses **completed bars only**; bar timestamps strictly < decision time
- Deterministic: identical event stream ⇒ identical outputs

Purpose: reduce low-quality entries without any grid/martingale behavior.
Output: `VirtualGate` module + unit tests (temporal contract + determinism) + wiring into unified policy.
</objective>

## <context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/PRD.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/TEST_CHECKLIST.md
@nautilus_gold_scalper/src/signals/mtf_manager.py
@nautilus_gold_scalper/src/strategies/gold_scalper_strategy.py
@nautilus_gold_scalper/src/risk/unified_risk_policy.py
</context>

## <tasks>

<task type="auto">
  <name>Task 1: Implement VirtualGate core (pure + deterministic)</name>
  <files>nautilus_gold_scalper/src/risk/virtual_gate.py</files>
  <action>
Create `VirtualGate` as a pure, deterministic component:
- Input: a snapshot of *completed* bar-derived features (do NOT access live/incomplete bar data)
- Output: `gate_ok: bool`, `gate_reason: str`, optional `gate_score: float`

Hard constraints:
- No state that depends on wall-clock time; only event timestamps and provided inputs.
- No randomness.
- Must never create “add-to-loser” behavior (it only gates entries).

Implementation note:
- Keep the initial rule conservative/simple. Prefer “fail-closed” if inputs are missing or timestamps violate contract.
</action>
  <verify>python -m compileall -q nautilus_gold_scalper/src/risk/virtual_gate.py</verify>
  <done>Module exists; deterministic evaluation function present.</done>
</task>

<task type="auto">
  <name>Task 2: Add timestamp/temporal contract checks</name>
  <files>nautilus_gold_scalper/src/risk/virtual_gate.py</files>
  <action>
Enforce the temporal contract explicitly:
- Given `decision_time`, any bar timestamp used must be strictly < `decision_time`.
- If violated: gate must fail-closed with a clear reason.

Do not rely on implied “bars are completed” assumptions; enforce with asserts/guards at boundaries.
</action>
  <verify>pytest -q</verify>
  <done>Temporal checks are enforced and covered by tests (next task).</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for temporal contract + determinism (B6)</name>
  <files>nautilus_gold_scalper/tests/test_risk/test_virtual_gate.py</files>
  <action>
Create tests matching TEST_CHECKLIST B6:
- Temporal contract: if any input bar timestamp >= decision time, gate must be NOT OK.
- Determinism: repeated evaluation with identical inputs yields identical outputs.

Keep tests pure (no backtest engine needed).
</action>
  <verify>pytest -q nautilus_gold_scalper/tests/test_risk/test_virtual_gate.py</verify>
  <done>Tests pass and protect no-look-ahead + determinism.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate VirtualGate into UnifiedRiskPolicy as entry-only gate</name>
  <files>nautilus_gold_scalper/src/risk/unified_risk_policy.py</files>
  <action>
Integrate VirtualGate into `UnifiedRiskPolicy.evaluate(...)`:
- If VirtualGate returns NOT OK ⇒ `can_open_new=False` with reason.
- Ensure it never affects exit/forced close paths.

Composition rule:
- “most restrictive wins” for entries.
</action>
  <verify>pytest -q</verify>
  <done>Unified policy includes VirtualGate and tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - `nautilus_gold_scalper/src/risk/virtual_gate.py`
    - `nautilus_gold_scalper/tests/test_risk/test_virtual_gate.py`
    - UnifiedRiskPolicy integration
  </what-built>
  <how-to-verify>
    1. Run: `pytest -q`
    2. Run: `mypy --strict`
    3. Confirm: VirtualGate is only consulted for new entries and cannot block forced close.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

## <verification>
- [ ] `pytest -q`
- [ ] `mypy --strict`

## <success_criteria>
- VirtualGate is deterministic and enforces completed-bar time contract.
- Tests protect B6 invariants.
- Policy integration is entry-only.

## <output>
After completion, create `.planning/phases/11-virtual-gate-safety-layer/11-03-SUMMARY.md`
</output>
