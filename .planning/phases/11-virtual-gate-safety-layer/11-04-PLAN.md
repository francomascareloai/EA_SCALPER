---
phase: 11-virtual-gate-safety-layer
type: execute
---

# Phase 11-04 — Integration backtest + falsification checks (ablation + hostile execution smoke)

## MANDATORY EXECUTION PROTOCOL

**ESTE PROTOCOLO DEVE SER SEGUIDO EM TODAS AS ACOES:**

### 1. Autonomous Loop (CRITIC ate GO)
```
Executar task → CRITIC review (opus) → GO?
                      ↓ NO
                Fix automatico → CRITIC review → loop (max 3x)
                      ↓ ainda NO-GO apos 3x
                Perguntar usuario
```

### 2. Quick Test/Backtest Apos Cada Fix
```bash
pytest -q
mypy --strict
python -m nautilus_gold_scalper.scripts.backtest.run_backtest --help
```

### 3. Anti-Hallucination
- Executar backtests com comando real do repo e salvar métricas em arquivo (sem inventar).
- Não dump de logs gigantes no chat: salvar em `DOCS/04_REPORTS/` e referenciar.

## <objective>
Run integration-level verification that the Safety Layer obeys invariants and improves **survival metrics** via:
- Force-close bypass scenarios
- DD breach bypass scenarios
- Gate composition precedence
- Ablation runs (add one component at a time)
- Hostile execution smoke (spread/slippage/latency multipliers)

Purpose: falsification-first evidence before scaling/optimization.
Output: a compact report file with metrics and a GO/NO-GO recommendation.
</objective>

## <context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/PRD.md
@DOCS/02_IMPLEMENTATION/PHASES/PHASE_4_INTEGRATION/20251225_VIRTUAL_GATE_PRD/TEST_CHECKLIST.md
@nautilus_gold_scalper/scripts/backtest/run_backtest.py
@nautilus_gold_scalper/configs/strategy_config_apex_mgc.yaml
</context>

## <tasks>

<task type="auto">
  <name>Task 1: Add minimal integration tests for bypass + precedence (C1/C2/C3)</name>
  <files>nautilus_gold_scalper/tests/test_integration/test_safety_layer_bypass.py</files>
  <action>
Implement integration tests aligned with TEST_CHECKLIST C1/C2/C3:
- C1 Force-close bypass: in_news_window=True AND time>=4:55 ET ⇒ forced close orders submitted.
- C2 DD breach bypass: trailing_dd_pct>=4.0 ⇒ HALT + flatten triggers; no further entries; exits proceed.
- C3 Precedence: “most restrictive wins” for entries; “must_flatten wins” globally.

Important:
- Keep tests minimal and deterministic.
- Prefer using existing strategy/risk components without spinning full backtest if test harness exists.
- If full engine is required, limit runtime and dataset scope.
</action>
  <verify>pytest -q nautilus_gold_scalper/tests/test_integration/test_safety_layer_bypass.py</verify>
  <done>Integration tests exist and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run ablation backtests (baseline vs +components)</name>
  <files>DOCS/04_REPORTS/DECISIONS/20251225_safety_layer_ablation.md</files>
  <action>
Run controlled backtests (same dates/data) and record survival-focused metrics:
- Baseline (existing behavior)
- +ExposureCaps
- +NewsGuard
- +VolatilitySpacing
- +VirtualGate

Rules:
- Change ONE component at a time.
- Record: termination count/probability proxy, max DD, MC95DD if available, number of trades, any Apex rule violations.
- If survival does not improve or invariants break: stop and mark NO-GO.

Write a compact markdown report with the metrics table and conclusion.
</action>
  <verify>python -m compileall -q nautilus_gold_scalper/scripts/backtest/run_backtest.py</verify>
  <done>Report file exists with real outputs pasted/summarized from runs.</done>
</task>

<task type="auto">
  <name>Task 3: Hostile execution smoke (spread/slippage/latency multipliers)</name>
  <files>DOCS/04_REPORTS/DECISIONS/20251225_safety_layer_hostile_smoke.md</files>
  <action>
Run a small hostile-execution smoke per TEST_CHECKLIST D2:
- spread×{2,3}
- slippage×{3,5}
- latency×{5,10}

Goal:
- Estimate whether termination probability worsens materially under stress.

Write a compact report with the chosen multipliers and observed effects.
</action>
  <verify>pytest -q</verify>
  <done>Smoke report exists with recorded run outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Integration tests for bypass/precedence
    - Ablation report and hostile smoke report
  </what-built>
  <how-to-verify>
    1. Run: `pytest -q`
    2. Run: `mypy --strict`
    3. Run: `python nautilus_gold_scalper/scripts/backtest/run_backtest.py --help`
    4. Review reports:
       - `DOCS/04_REPORTS/DECISIONS/20251225_safety_layer_ablation.md`
       - `DOCS/04_REPORTS/DECISIONS/20251225_safety_layer_hostile_smoke.md`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

## <verification>
- [ ] `pytest -q`
- [ ] `mypy --strict`
- [ ] Reports exist with real metrics

## <success_criteria>
- Invariants verified at integration level.
- Ablation shows survival improvement or clearly falsifies the approach.
- Hostile smoke does not reveal immediate failure modes.

## <output>
After completion, create `.planning/phases/11-virtual-gate-safety-layer/11-04-SUMMARY.md`
</output>
