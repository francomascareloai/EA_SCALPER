## Backtest vs Live News Gate - 2025-12-20 23:18

- **Separate news backtest vs live behavior** - Keep news gating deterministic in backtests and avoid changing live behavior until backtests are validated. **Problem:** News pipeline currently mixes concerns (backtest determinism vs live feed readiness) and the coverage gate semantics may be too strict/too lenient for years 2015–2025. **Files:** `nautilus_gold_scalper/src/strategies/gold_scalper_strategy.py:1127`, `nautilus_gold_scalper/scripts/data/validate_hf_forex_factory_calendar.py:360`, `nautilus_gold_scalper/configs/strategy_config.yaml:54`. **Solution:** Add an explicit “mode” switch for news behavior (backtest/live), keep using `bar.ts_event` in backtests, and tighten/verify coverage gate on the validated USD calendar for 2015–2025.

## Optimizer Robustness Refinements - 2025-12-25 16:03

- **Finalize optimizer configs and type gate** - Version canonical fast/full optimization configs and validate optimizer paths by default in strict type checks. **Problem:** Optimization could not be run reproducibly from scratch because the recommended YAML configs were not versioned, and the default `mypy --strict` gate did not cover optimizer/scripts (risk of silent regressions). **Files:** `nautilus_gold_scalper/configs/grids/smc_optimization_fast.yaml:1`, `nautilus_gold_scalper/configs/grids/smc_optimization_full.yaml:1`, `mypy.ini:5`. **Solution:** Commit `smc_optimization_fast.yaml` + `smc_optimization_full.yaml`, and extend `mypy.ini` `files=` list to include optimizer/scripts modules used for production optimization.
